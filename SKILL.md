---
name: instagram-hk-leads
description: Find Hong Kong male personal Instagram accounts through search, places, followers, following, and Cantonese comments using ego lite (ego-browser). Maintain a local SQLite list, expand promising seeds, and improve screening from human feedback. Use for Instagram 香港客户寻找、种子扩展、审核名单、持续找人 and resuming this workflow. Produces account lists; excludes messaging and engagement campaigns.
---

# Instagram 香港客户寻找

从 Instagram **搜索或地点**寻找起点，沿合格账号的关注、粉丝和帖子下的粤语评论继续寻找。交付名单；通过用户反馈逐渐减少逐个审批。实现先求简单可用，每次真实使用后修正规则和读取方法。

## 使用条件与数据位置

- 适用于能够读取本 skill、执行 Python 3.9+ 和调用 **[citrolabs/ego-lite](https://github.com/citrolabs/ego-lite)** 的 agent；不依赖某个 agent 的聊天记忆。Python 工具仅用标准库。
- 浏览器操作使用安装环境中配套的 `ego-browser` skill。其调用版本、Instagram 当次登录状态及页面可见范围必须以实际结果为准；阅读 [ego-browser.md](references/ego-browser.md)。不能把文档示例当作本机已验证能力。
- 为这项找人任务选定**同一条 SQLite 绝对路径**，所有 agent 和后续运行复用它。默认在用户工作目录下的 `instagram-hk-leads-data/leads.sqlite3` 创建；将绝对路径写入本任务交付说明。技能文件与运行数据分开放，升级 skill 不覆盖数据。
- 所有命令显式传 `--db`；具体 JSON 格式、命令和导出方式见 [database.md](references/database.md)。开始新的数据库用 `init`；继续已有数据库先看 `stats`、当前模式及规则，再读 `next`。
- 已有实例缺少 ego-browser 或登录条件时，可以继续整理已保存资料和用户反馈；说明缺少的具体条件。没有实际读取到的账号、证据或列表不能编造，也不能拿示例凑满数量。

## 已确认的筛选要求

1. 目标是**在香港生活的男性普通个人账号，不限制年龄**。不另外添加收入、职业、粉丝数、公开程度、活跃度或消费能力门槛。
2. 繁体粤语、简介明确写香港、香港旗帜或相关表情，任一命中即可进入候选；从合格种子的关系中发现的人也可进入候选，没有这些文字不直接排除。
3. 分开判断 `personal`、`male`、`resident_hk`，每项为 `yes` / `no` / `unknown`。依据公开自述及本人发布的帖子；不把头像外观、姓名、口音或单张人物照当作确定身份的证据。
4. 香港线索不自动等于现居香港。准备自动通过时需有更充分、可追溯的居港依据，例如明确现居自述或多次不同日期的香港日常生活内容；明显旅游、移居或资料冲突须保留不确定性。
5. “普通个人”按内容和用途判断，排除商家、机构、广告和搬运为主的账号。没有必要逐帖做原创鉴定；只在转发、内容主体或明显矛盾影响判断时核对，避免用他人的生活证明账号主人的身份。
6. 私密、帖子少、列表不可见、加载失败与不符合条件分别记录。读不到是资料不足或入口不可用，不据此生成负面审核样本。

## 寻找循环

### 1. 寻找起点

没有可扩展种子时，使用 Instagram 当前页面真实提供的**搜索或地点入口**。从香港、Hong Kong、实际地区或地点及当地生活主题出发，读取搜索结果或地点相关帖子，检查其作者。

使用不同地区、地点或生活主题补充起点，记录查询词、入口链接和发现时间。不要为了寻找而修改浏览器定位、平台推荐设置或账号资料；某种搜索形式不可用时改用另一个实际可见入口。

### 2. 收集与排序

将实际发现的账号、三项判断、简短理由、证据和来源写入数据库，再产生稳定编号的审核批次。**首批 20 个**是审核目标，不是总人数、搜索层数或平台安全限额；不足 20 个时提交实际数量并解释原因。

保留每一条真实来源关系，并明确方向：

- `following`：种子关注此人。
- `follower`：此人关注种子。
- `comment` / `reply`：此人在种子的某个帖子下留言或回复。
- `search` / `place`：从指定搜索或地点入口发现。
- `mention`：在已读取内容中被提及，仅作线索，不能当作发言人。

**出现越多，排序权重越高，但先消除重复和相关来源：**

- 重新加载、重复导入、同一种子反复留言，不算多个独立来源。
- 不同合格种子都指向同一账号时，提高排序与关系可信度；保留各方依据，便于用户核对。
- 多人提及支持“值得查看”，不单独证明男性或居港；不能用出现次数覆盖用户的排除决定。
- 统计“新发现”和“多个来源再次发现”两种结果，不重复提交已审核账号；有新的矛盾证据时明确作为复核提出。

### 3. 分开管理客户与种子

通过筛选的账号进入名单，也具备成为新种子的资格。是否优先扩展，还要看实际可读取的入口、已发现的新合格账号数量与重复程度；一个人符合客户条件但不好继续找人，仍保留在客户名单中。

轮流从多个种子寻找，优先处理实际带来更多新合格账号的入口。不要仅因粉丝多就优先，也不要把某条路径发现的人全部默认为香港男性。一个分支重复较多时切到其他种子或补充搜索／地点起点，仍保留原分支进度。

### 4. 展开三类入口

对选中的种子，按当前实际可用入口检查：

1. 他关注的人。
2. 关注他的人。
3. 他发布的帖子下的粤语评论者及页面可展开的回复者。

选择若干不同帖子的实际可见评论，不把一篇帖子的首屏当作全部关系。普通话、英文或仅表情的发言不能证明对方不居港；若另有香港或关系线索，仍可检查主页。

每发现一人，就回到“收集与排序 → 判断／审核 → 名单与种子”。**不设固定层数**；将可继续展开的账号留在数据库，避免对同一账号和入口反复从头读取。

## 人工反馈与逐渐减少审批

数据库支持 `learning`、`assisted`、`automatic` 三种模式；默认从 `learning` 开始。

| 模式 | 行为 |
|---|---|
| `learning` | 首批提交 20 个候选；前期可多次审核、没有固定轮次上限，人工通过的账号可展开。 |
| `assisted` | 对多次人工审核已经支持的清晰规则自动通过和展开，把模糊、新情况或冲突留给用户。 |
| `automatic` | 符合已总结规则的账号自动入选并展开；不确定的保留待核实，不为每个账号打断用户。 |

用户可直接回复“1、3 符合，2 是商家，4 不确定”。将其映射到**明确的 batch_id 和固定编号**；只有多个批次均可能对应时，才问清楚，不能猜错账号。原因未给出时如实留空或记“用户未说明”，不编造原因。

记录用户原始决定，保留模型当时预测，并把不符合的原因尽量拆到男性、居港、个人用途等具体条件。纠正旧决定要追加新的人工记录，而非抹掉历史；新的浏览结果不能覆盖人工决定。

**边做边学的具体动作：**

1. 每次收到反馈，先入库，再比较本轮预测与用户判断；统计符合、不符合、不确定和分歧原因。
2. 总结有效正反例、需要调整的规则或某条寻找路径的价值；通过 `learn` 保存新版本、调整理由及所依据的人工反馈。自动判断与重复出现次数不充当人工真值。
3. 用新规则重新检查既有人工样本，尤其是先前判断错的例子；改进应减少已知错误，不能靠放宽用户目标来提高通过数量。
4. 多次审核后，依据不同来源样本的实际一致程度、分歧类型及证据覆盖逐步减少审批。**不能因为已经审核 20 人、出现次数多或 agent 自称有把握就切换模式**；通过 `mode` 记录转换及理由，不承诺任何固定轮数后即可准确免审。
5. 自动模式允许持续执行，保留用户纠错与恢复人工模式的入口；用户新纠正的规则优先，必要时复核受影响的自动入选账号与其扩展来源，不把整个后续网络一律判错。

不因每个小调整反复请求批准；筛选目标改变、读取范围扩大或新增对外操作需要用户明确指示。这里的“学习”是保存审核样本、改进筛选规则和寻找顺序，不能称为已经训练或更新了底层模型。

## 数据读取与继续条件

- 搜索、地点入口，以及每个种子的关注、粉丝、帖子发现和每篇帖子的评论分别记录“待处理／进行中／本次完成／不可用”及最后进度；发现关系与账号判断分开存放。读完已知帖子的评论，不等于已查看该账号所有可用帖子。
- “本次完成”只表示已经处理本次可见内容，不宣称完整抓取全部关注者或全部评论。没有新数据、页面不可见、加载受限与真正完成要区分。
- 数据库写入成功后再标记读取进度；中断时重新观察页面、按已存账号去重后接续，不保存或复用失效的页面控件编号。
- 遇到登录失效、验证、平台限制或用户接管浏览器时保存进度并暂停对应读取；不绕过限制，不持续重试，也不宣称某种速度保证不受限制。
- 持续寻找发生在当前获授权的运行中；技能本身不创建定时任务或后台常驻进程。用户暂停、当前运行结束或没有可继续读取的入口时，交付已有结果和接续位置。
- Instagram 操作范围为读取。查看关注列表不执行关注，查看评论不发评论；名单任务不包含点赞、私信、申请查看私密账号或修改账号设置。

## 交付与验收

使用 `list` / `batch` / `export` 交付实际记录，至少包含主页链接、判断理由、人工或自动状态、发现来源及独立来源数量。候选审核名单和已通过名单分开标注。

每次结束说明：新增候选数、人工／自动通过数、仍待核实数、重复发现数、当前规则版本和暂停／接续位置；只在收到新反馈或规则变化时解释学习结果，不刷无变化的状态。

首次可用验收包括：真实读取一个搜索或地点入口与一个账号；在有合格种子后核对实际可用的三类扩展入口；写入后读回、同一来源重复导入不增权、不同种子来源增权、用户反馈保留且后续 agent 可接续。未完成的真实读取须明确写“未实测”，本地脚本测试不能替代 Instagram 实测。
